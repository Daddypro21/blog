{% extends 'base.html.twig' %}

{% block  body %}

<p>Bonjour je suis la page d'accueil :{{title}}</p>

{% for post in posts %}
<div class="container">
<div class="row">
<div class="">
<div class="card  mb-3" >
  <small>{{post.created_at}}</small>
  <img class="card-img-top" src="..." alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">{{post.title}}</h5>
    <p class="card-text">{{post.content}}</p>
    
  <a href={{linkPost}} class="btn btn-secondary">Retour</a>
  </div>
</div>
</div>
</div>
</div>
 {% endfor %}

{% endblock %}